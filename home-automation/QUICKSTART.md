# Быстрый старт

Это руководство поможет быстро запустить систему мониторинга умного дома.

## Шаг 1: Настройка Supabase (5 минут)

1. Перейдите на [supabase.com](https://supabase.com) и создайте аккаунт
2. Создайте новый проект
3. Скопируйте Project URL и anon public key из Settings → API
4. Перейдите в SQL Editor
5. Скопируйте и выполните содержимое файла `supabase-schema.sql`
6. Нажмите RUN - база данных готова!

## Шаг 2: Настройка приложения (2 минуты)

```bash
# Скопируйте файл с примером настроек
cp .env.local.example .env.local

# Откройте .env.local и заполните:
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

## Шаг 3: Установка и запуск (2 минуты)

```bash
# Установите зависимости
npm install

# Запустите в режиме разработки
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## Шаг 4: Добавление тестовых данных (опционально)

### Вариант A: Через Node.js скрипт

1. Откройте `scripts/generate-test-data.js`
2. Замените `YOUR_SUPABASE_URL` и `YOUR_SUPABASE_KEY`
3. Запустите:

```bash
node scripts/generate-test-data.js
```

### Вариант B: Через SQL

```sql
-- Добавить тестовое показание температуры
INSERT INTO sensor_readings (sensor_type, value, unit, location)
VALUES ('temperature_indoor', 22.5, '°C', 'Гостиная');

-- Добавить несколько показаний
INSERT INTO sensor_readings (sensor_type, value, unit) VALUES
  ('temperature_indoor', 22.5, '°C'),
  ('temperature_outdoor', 5.2, '°C'),
  ('humidity_indoor', 45, '%'),
  ('power_consumption', 3.2, 'кВт');
```

### Вариант C: Через API

```bash
curl -X POST http://localhost:3000/api/sensors \
  -H "Content-Type: application/json" \
  -d '{
    "sensor_type": "temperature_indoor",
    "value": 22.5,
    "unit": "°C",
    "location": "Гостиная"
  }'
```

## Шаг 5: Интеграция с контроллером (опционально)

Если у вас есть устройство (Raspberry Pi, Arduino, ESP32 и т.д.):

### Python вариант:

```bash
cd examples
# Установите зависимости
pip install requests

# Настройте параметры в controller-integration.py
# Запустите
python controller-integration.py
```

### Node.js вариант:

```bash
cd examples
# Настройте параметры в controller-integration.js
# Запустите
node controller-integration.js
```

## Проверка работы

После запуска вы увидите:

1. **Главная страница** - карточки с последними показателями датчиков
2. **Графики** (`/charts`) - история показаний за выбранный период
3. **Правила** (`/rules`) - список активных правил мониторинга
4. **Управление** (`/control`) - управление отоплением и температурой

## Настройка прогноза погоды (опционально)

1. Зарегистрируйтесь на [OpenWeatherMap](https://openweathermap.org/api)
2. Получите бесплатный API ключ
3. Добавьте в `.env.local`:

```env
NEXT_PUBLIC_WEATHER_API_KEY=your-api-key
NEXT_PUBLIC_WEATHER_CITY=Moscow
```

4. Перезапустите приложение

## Структура данных

### Отправка показаний датчика

```javascript
POST /api/sensors
{
  "sensor_type": "temperature_indoor",  // Тип датчика
  "value": 22.5,                        // Значение
  "unit": "°C",                         // Единица измерения
  "location": "Гостиная"                // Местоположение (опционально)
}
```

### Типы датчиков

- `temperature_indoor` - температура в доме
- `temperature_outdoor` - температура снаружи
- `temperature_heating_supply` - температура подачи отопления
- `temperature_heating_return` - температура обратки
- `humidity_indoor` - влажность в доме
- `humidity_outdoor` - влажность снаружи
- `pressure_outdoor` - атмосферное давление
- `power_consumption` - потребление электроэнергии
- `voltage` - напряжение в сети
- `water_flow_cold` - расход холодной воды
- `water_flow_hot` - расход горячей воды
- `gas_consumption` - расход газа

## Управление через команды

Контроллер читает команды из таблицы `controller_commands`:

```sql
-- Посмотреть ожидающие команды
SELECT * FROM controller_commands
WHERE status = 'pending'
ORDER BY created_at;

-- Отметить команду как выполненную
UPDATE controller_commands
SET status = 'acknowledged', executed_at = NOW()
WHERE id = 'command-id-here';
```

## Примеры команд

### Включить отопление

```javascript
await supabase.from('controller_commands').insert({
  command_type: 'enable_heating',
  parameters: { enabled: true },
  status: 'pending'
});
```

### Установить температуру в комнате

```javascript
await supabase.from('controller_commands').insert({
  command_type: 'set_room_temperature',
  parameters: { room_id: 'room-uuid', temperature: 23.0 },
  status: 'pending'
});
```

## Решение проблем

### Не отображаются данные

1. Проверьте консоль браузера (F12)
2. Убедитесь, что Supabase URL и ключ указаны правильно
3. Проверьте, что схема БД создана
4. Добавьте тестовые данные

### Ошибка подключения к Supabase

1. Проверьте Project URL (должен начинаться с `https://`)
2. Проверьте, что используется anon (public) key, а не service role key
3. Убедитесь, что проект Supabase активен

### Не работает Real-time обновление

1. Real-time включен по умолчанию в Supabase
2. Проверьте, что таблица `sensor_readings` имеет Row Level Security (можно отключить для тестов)
3. Откройте Supabase Dashboard → Database → Replication и убедитесь, что таблицы включены

## Следующие шаги

1. Настройте правила мониторинга в разделе "Правила"
2. Добавьте свои комнаты в разделе "Управление"
3. Интегрируйте реальные датчики через API
4. Настройте Telegram уведомления (в разработке)
5. Разверните на Vercel для доступа из интернета

## Полезные ссылки

- [Документация Next.js](https://nextjs.org/docs)
- [Документация Supabase](https://supabase.com/docs)
- [Документация Recharts](https://recharts.org)
- [OpenWeatherMap API](https://openweathermap.org/api)

## Поддержка

Если возникли вопросы:
1. Проверьте README.md для подробной информации
2. Создайте Issue в репозитории
3. Проверьте логи в консоли браузера

Приятного использования! 🏠🔥💡
